- 합격 입고처리버튼 클릭시
     -> 자재발주테이블 상태값 6으로 변경. (트랜젝션 처리)
     -> 자재입고테이블 인서트. 
	(필요값: 입고번호(시퀀스), 입고수량, 입고완료일(sysdate), 입고담당자,
		자재발주코드, 자재코드)
     -> 자재재고테이블 ROT별로 인서트 (프로시저 절차형 입고테이블 인서트 후에 처리)
	(필요값: 자재ROT(지정값+시퀀스), 재고수량, 
		유통기한(sysdate+자재테이블유통기한(월)),
		자재코드, 자재입고번호) 
	(ROT 별 수량 : 생두->  5개)
	한주 발주요청수량 어느정도인지 보고 정하기.
	생두별로 5~6bag
- 불합격 반품처리버튼 클릭시
     -> 자재발주테이블 상태값 7으로 변경. (트랜젝션 처리)
     -> 자재반품테이블 인서트. 
	(필요값: 반품번호(시퀀스), 반품수량, 반품완료일(sysdate), 반품담당자,
		자재발주코드, 자재코드)

- 부자재 입고처리버튼 클릭시
     -> 자재발주테이블 상태값 6으로 변경. (트랜젝션 처리)
     -> 자재입고테이블 인서트. 
	(필요값: 입고번호(시퀀스), 입고수량, 입고완료일(sysdate), 입고담당자,
		자재발주코드, 자재코드)
     -> 자재재고테이블 ROT별로 인서트 (프로시저 절차형 입고테이블 인서트 후에 처리)
	(필요값: 자재ROT(지정값+시퀀스), 재고수량, 
		유통기한(null),
		자재코드, 자재입고번호) 
	(ROT 별 수량 : 원두봉투->  , 포장박스->  )
	한주 발주요청수량 어느정도인지 보고 정하기.
	봉투별로 15개~18개 / 박스 60박스

- 트랜젝션 처리 완료하기.
- 프로시져 사용해서 입고처리마무리하기.

- 자재관리 페이지 등록에 자재코드 거래처에따라 자동으로 들어가도록 만들기.